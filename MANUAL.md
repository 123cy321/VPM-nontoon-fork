# NonToon (Fork) 用户手册

> 对应版本：着色器 **0.3.7** ／ 工具包 **0.4.7**（2026-09）
> 仓库与更新：**https://catandling.github.io/VPM-nontoon-fork/vpm.json**
> 想快速上手 → 看 [README](README.md)；想搞清楚"这东西到底怎么用、有什么坑" → 看本手册。

---

## 目录

1. [这是什么](#1-这是什么)
2. [安装](#2-安装)
3. [功能总览](#3-功能总览)
4. [常用组件](#4-常用组件)
5. [注意事项与性能](#5-注意事项与性能)
6. [与官方 NonToon 的兼容性](#6-与官方-nontoon-的兼容性)
7. [常见问题](#7-常见问题)
8. [已知限制](#8-已知限制)

---

## 1. 这是什么

给 **VRChat avatar** 用的卡通着色器，基于 [lilxyzw/NonToon](https://github.com/lilxyzw/NonToon) 改进。

官方版本停在 0.1.3 之后没再动。这个分支做的事情：

- **修官方留下的问题**：半透明、透明排序、侧光下渐变打架、受光方向偏移、MatCap 在 VR 里的视差等
- **加大量的可调项**：光照上下限、单色光照、无光照、1/2/3 层阴影色、逐像素阴影遮罩、Emission 全套、描边、细节贴图、距离淡出……
- **自带光源 / 自带阴影**：地图太黑、地图的光不对劲的时候，让 avatar 不依赖地图的光（见 [§4](#4-常用组件)）
- **界面中文**：165 条面板属性里已汉化 117 条（0.3.7 起自动生效，无需手动切语言）
- **lilToon 材质一键迁移**

**环境**：Unity 2022.3 ／ **BiRP**（VRChat 用的渲染管线）／ PC 与 Quest 都可用
（Quest 上"实时光源"不可用，见 §5）

---

## 2. 安装

### 一键

打开 **https://catandling.github.io/VPM-nontoon-fork/** → 点页面上的按钮 → VCC/ALCOM 会弹「添加仓库」确认框。

### 手动

在 ALCOM / VCC 里找到「添加仓库 / Add Repository」，粘贴 **listing 地址**（不是 zip 地址）：

```text
https://catandling.github.io/VPM-nontoon-fork/vpm.json
```

### 两个包，装哪个

| 包 | 要不要装 | 是什么 |
|---|---|---|
| **`NonToon (Fork)`** | **必装** | 着色器本体 |
| **`NonToon (Fork) Tools`** | 建议装 | lilToon 材质转换器 + 两类光源组件 + 自阴影烘焙（纯可选，不影响着色器） |

> 装工具包会**自动带**着色器；只装着色器**不会**带工具包。
> 依赖是单向的：工具包 → 着色器。

---

## 3. 功能总览

以下都是材质面板上的分组（打开任意 NonToon 材质即可看到）。

### 主色与基础

| 功能 | 说明 |
|---|---|
| 渲染模式 | 不透明 / 镂空（Cutout）/ 半透明 |
| 主贴图 + 共享遮罩 + 共享渐变 | 一张遮罩图可以给所有模块按通道取用 |
| 法线贴图 + 粗糙度 + 裁剪阈值 | 支持从法线贴图里取粗糙度 |
| 反照率遮罩（RGBA 四通道）| 一个模块控四个区域 |

### 阴影（1/2/3 层）

- **三层阴影色**，每层各有：颜色、边界（Border）、模糊（Blur）
- **逐像素阴影遮罩**：用一张图的三个通道分别控制三层阴影的强度/边界/模糊
- **阴影主色强度**、**边界颜色与范围**、**AA 强度**（抗锯齿）
- lilToon 的这些项**可以从 lilToon 材质 1:1 转换过来**

### 光照调整

| 项 | 作用 |
|---|---|
| 光照上下限（Min/Max Limit） | 把受光强度压进一个范围，控制整体明暗对比 |
| 单色光照（Monochrome） | 去掉光的颜色，只用明暗（避免被地图灯光染色） |
| 无光照（As Unlit） | 完全不吃光，永远满亮 |
| 阴影方向偏置 | 修"侧光下渐变打架" |

### 其他模块

描边（含 Offset Factor/Units、从顶点色取宽度）、边缘光、背面光、MatCap（含 VR 视差）、
发丝高光、边缘阴影（RimShade）、细节贴图（4 通道）、距离淡出（远处自动简化）、
Nearer（VR 里防穿模）、**Emission 全套**（颜色/贴图/混合模式/混合遮罩/主色强度）、
毛发（NonToonFur）。

### 自有光源（自带光 / 自带阴影）

这是本分支相对官方版最大的新增能力，详见下一节。

---

## 4. 常用组件

工具包提供**两类**组件，解决的问题一样（"我的 avatar 不该被地图的光决定长相"），但**代价和适用场景完全不同**。
选错会白白掉 VRChat 性能等级，所以先想清楚要哪一种。

| | **① Avatar 光源** | **② 自有光源** |
|---|---|---|
| 菜单 | `GameObject ▸ NonToon ▸ ① 创建 Avatar 光源（不用改材质）` | `GameObject ▸ NonToon ▸ ② 创建自有光源（需要在材质启用）` |
| 要动材质吗 | **不用**。lilToon / Poiyomi / Standard 任何着色器都吃 | **要**。材质得是 NonToon；组件会自动帮你打开开关 |
| 原理 | 真的挂一盏 Unity `Light` | 在着色器里加一路**私有光**，只有这个材质看得见 |
| 阴影 | Unity 实时阴影，**跟着姿势变** | **烘焙**的阴影图（固定姿态），永远可见 |
| 可见范围 | **会照亮周围世界与其他玩家** | 只照自己，别人看不到你的光 |
| 代价 | 占 VRChat 的 `Lights` 计数（PC 上最高只能到 **Poor**）；每个看到你的人都付这份开销 | 着色器采样开销（默认很低，见 §5） |
| Quest | 基本不可用 | **可用** |
| 多人叠加 | 一堆人同时开 → 照成一片白 | 各自独立，互不影响 |

### ① Avatar 光源（不用改材质）

- 会在你指定的节点下创建一盏真实的 Unity 光源，并**自动打开** `Receive Shadows`
- 默认 Culling Mask 是 **PlayerLocal**（只照本地玩家层），避免照到别人
- 支持色温（直接用 Unity 的 `Light.colorTemperature`）
- **适合**：地图很黑想临时补个光、或根本不用 NonToon 着色器（比如 Poiyomi 用户）
- **不适合**：在意性能等级、要上 Quest、或者不想照亮周围环境

### ② 自有光源（需要在材质启用）

材质面板里对应的是「Self Light」组：

| 参数 | 说明 |
|---|---|
| **自有光源** 开关 | 打开后着色器多一路私有光（组件会自动写入，你不用手点） |
| **只接受光源编号**（Light ID） | 0 = 任意。同一个模型上可以并存多路自带光，用编号让材质"只认哪一路"，互不打架。组件写入材质时会自动盖章编号 |
| 颜色 / 使用色温 / 色温 (K) | 1000–20000K，6500 ≈ 中性白 |
| 强度 / 方向（世界空间，指向光源） | 方向就是"光从哪来" |
| **只由它照亮** | 世界光、环境光、光照贴图、天空盒反射**全部丢掉**，avatar 在任何地图里长得一样 |
| **屏蔽地图环境光**（0–1 滑块） | 只想屏蔽环境光、但保留地图的太阳时用（近似做法） |
| 匹配世界光颜色 / 方向 | 探测地图环境光的颜色与主光方向，让自己的光跟着变。**读的是着色器里本来就有的数据，不额外花一分钱** |
| 阴影强度 / 阴影颜色 / 阴影强度遮罩 | 阴影处染什么色、逐像素控制哪里影子深 |
| **接收遮罩** | 逐像素控制哪里收影子（比如眼白不想被刘海投到，就涂黑） |
| **PCSS 软阴影 + 画质四档** | 低 20 / 中 36 / 高 60 / 极高 96 次采样每像素 |
| 阴影硬化（Clamp） | 把软边压成硬边，动画风用 |
| 阴影距离 | 离相机超过设定距离自动关掉 |
| **自备阴影贴图（Map 组）** | 自己提供灰度深度图 + 原点/三轴/半宽高/远近平面 |

**推荐配置**（地图没灯时）：

1. 勾 **「只由它照亮」** → avatar 在任何地图里长得一样
2. 只想屏蔽环境光 → 用 **「屏蔽地图环境光」** 滑块
3. 点组件上的 **「烘焙自阴影并写入材质」** → PCSS 画质可到极高、贴图可到 2048

组件上有两个按钮：**「只同步参数（不烘焙）」**（只把参数写进材质，不生成阴影图）和
**「烘焙自阴影并写入材质」**（CPU 光线投射生成深度图后写入）。

### ③ 自阴影烘焙（在 ② 组件里）

- 在编辑器里**按当前姿势**烘焙一张深度图，写进材质
- 影子的代价几乎为零（就是采样一张图），Quest 也能用
- **影子是"烘焙那一刻的姿态"**：VRChat 里做不到实时自阴影（没法带相机/RT）
- 想换姿势重新烘焙即可

---

## 5. 注意事项与性能

### 性能：默认全是最省的那档

NonToon 的本分就是低消耗。**每像素自阴影采样**：

| 配置 | 采样 / 像素 |
|---|---|
| 自有光源关（**默认**） | **0** |
| 自有光源开、强度 0（只要光不要影） | **0** |
| 自有光源开、PCSS 关（硬阴影） | **1** |
| PCSS **低（默认）** | **20** |
| PCSS 中 / 高 / 极高 | 36 / 60 / 96 |
| 接收遮罩 / 阴影强度遮罩 | 各 +1（**默认 0 = 不采样**） |

采样**只在光源包围盒内 + 阴影距离之内**才发生。组件面板会直接把当前预算写在上面。

### 实时光源（① 与 ② 的实时光源模式）的代价

- 额外渲染一张阴影贴图，**每个看到你的人都付这份开销**
- 占 VRChat 的 `Lights` 计数。PC 上 Excellent / Good / Medium 都要求 `Lights = 0`，
  只有 Poor 允许 1 —— 也就是说**挂一盏实时光，性能等级最高只能到 Poor**
- Quest 端实时光基本不可用

### 其它注意

- **② 的阴影是静态的**：换姿势/换衣服后要重新烘焙
- **① 和 ② 不要同时开**：会双份打光。② 组件的实时光源模式会自动把材质里的自有光源关掉，并在面板上警告
- **自备阴影贴图的约定**：灰度 = 离光源近平面的归一化距离；贴图要**非 sRGB、不压缩、Clamp、无 mip**。烘焙出来的图就是这套约定
- **渲染队列**：`NonToon.scshader` 的两个 SubShader 都**没有**声明 `Queue` 标签（`NonToonFur` 有 `AlphaTest`），
  队列靠编辑器写入 → **脚本创建或复制出来的材质不会自动带队列**，半透明可能显示异常，手动物理一下 Queue 即可
- **语言**：0.3.7 起会自动把中文表补齐成当前语言那一份，**你不需要手动去 Language 下拉框选简体中文**。
  但面板里仍有约 48 条英文（`SrcBlendRGB` / `ZWrite` / `Ref` / `Comp` / `Pass` / `Cull` / `AlphaToMask` 等），
  这些是 ShaderCore 的底层属性，本包补不到

---

## 6. 与官方 NonToon 的兼容性

结论：**升级官方版不会破坏你已经做好的东西**。下面是实测依据。

### 包 id 相同 → 是"升级"，不是"并存"

本包的 `package.json` 里 `name` 与官方**完全相同**（`jp.lilxyzw.nontoon`），
所以 VPM 把它当成官方版的**新版本**：装上去就是替换官方版，**不会**在工程里出现两个 `Shader "NonToon"`。

显示名带 `(Fork)` 只是为了让你在包列表里分得清，它不参与依赖解析。

### 着色器名与资源 GUID 都没变

- 着色器名仍是 `Shader "NonToon"` / `"NonToonFur"`，`Shader.Find("NonToon")` 之类按名查找的代码照常工作
- **实测（2026-09）**：与官方版 0.1.3 共有的 **78 个带 `.meta` 的文件，GUID 全部一致**；
  `Shaders/NonToon.scshader` 的 GUID 是 `78361b0b760724141a2d2c09100cf00f`，**与官方逐字符相同**
  → **你已有的材质、预制体、场景不会断引用**，不需要重新指定着色器

### 材质参数：官方有的全在，新的默认不影响观感

- **实测**：官方 0.1.3 的 **92 条属性，本包一条都没少**；本包另外新增了 75 条
- 新增属性默认值都是"不影响原有观感"的：自有光源默认**关**、PCSS 默认低、遮罩默认不采样
- 也就是说：把官方工程的材质升级上来，**参数不会丢**，而画面差异来自本分支刻意修的那些光照/半透明问题
  （这正是这个分支存在的理由）
- 官方 0.1.3 的 **10 个模块**在本包里全在（details / distancefade / hairspecular / lighten / matcaps /
  nearer / rimlight / rimshade / shade / specular），另外新增 3 个：
  `emission`、`selflight`、`shadowcolor`

### 从不同版本升上来

| 你的起点 | 怎么做 |
|---|---|
| 官方 0.1.3 或本分支 0.1.4+ | 直接在 ALCOM/VCC 里升级即可 |
| 本分支 **0.1.10 及更早** | 那时候着色包里**内嵌了转换器**，覆盖安装可能残留出两个转换器。把 `Packages/jp.lilxyzw.nontoon` 整个删掉、让包管理器重装一次 |
| 0.3.6 及更早升到 **0.3.7** | 见 [§7](#7-常见问题)：可能有一次组件变 Missing Script，重挂一次即可 |

### 装回官方版 / 回退旧版本

- 因为包 id 相同，你在 ALCOM/VCC 里**装官方版就会替换掉本分支**（反之亦然），两者不会共存
- 本 forks 的 listing 里**保留了所有历史版本**（着色包从 0.1.6 起，共 15 个），可以指定版本回退
- **旧的已发布版本不会被删除**——VPM 靠版本列表做依赖解析，删了会破坏用 Git 管理的工程

### 与其它着色器的关系

| | 说明 |
|---|---|
| **lilToon** | 工具包提供**单向**转换器：`Tools ▸ lilToon → NonToon 转换器`。**生成新材质，绝不动你原来的 lilToon 材质** |
| **Poiyomi / Standard 等** | ① Avatar 光源**与着色器无关**，任何着色器都能用；② 自有光源只对 NonToon 材质有效 |
| **ShaderCore** | 本包依赖 `jp.lilxyzw.shadercore ≥ 0.1.9`（VPM 自动带走）。ShaderCore 0.1.12 起不再附带简体中文表，本包会在缺失时自动补 |

---

## 7. 常见问题

**Q：装上去会不会和官方 NonToon 并存？**
不会。包 id 相同，所以是**升级**官方版（见 §6）。

**Q：升级到 0.3.7 后，场景里某个组件变成 `Missing (Mono Script)`？**
0.3.6 及更早的包里，「① Avatar 光源」这个脚本文件**漏带了 `.meta`**，于是 Unity 给每台机器生成的是
不同的 GUID —— 你挂好组件之后换机器打开、或重装这个包，引用就会断。0.3.7 已修。
**处理**：把 Missing 的组件删掉，重新挂一次（`GameObject ▸ NonToon ▸ ① 创建 Avatar 光源`）并重填参数。
从 0.3.7 起 GUID 已固定，以后升级不会再断。

**Q：我明明选了简体中文，为什么面板里还有英文？**
0.3.7 起不需要你手动选了：中文 Windows 上 ShaderCore 的默认语言是 `zh-CN`，旧版本包只带了 `zh-Hans.po`，
两者对不上，所以默认状态下 165 条属性里只有 35 条是中文；0.3.7 会自动补齐，现在 **117 条**。
剩下约 48 条是 ShaderCore 自己的底层属性（见 §5），本包补不到。

**Q：转换后看着不太一样？**
转换是近似的（MatCap、边缘光、距离淡出、毛发、描边尤甚）。请看转换报告里标了「近似」的条目，
或参考工具包里的 `Mapping.md`。

**Q：Quest 上能用吗？**
着色器侧功能可以；**实时光源**（① Avatar 光源、② 的实时光源模式）基本不可用。② 的烘焙自阴影可用。

**Q：老是提示 ShaderCore 模块缺失？**
装完本包会在编辑器加载时自动补注册并重导着色器（ShaderCore 的模块表只扫描一次，升级上来会静默丢模块）。
在控制台看到 `[NonToon] …补注册模块` 就是它在干活。

**Q：地图里的光很难看，我不想每次都去调材质？**
用 ② 自有光源的「匹配世界光颜色 / 方向」，或者干脆勾「只由它照亮」让 avatar 与地图光完全解耦。

**Q：从很老的版本升上来，菜单里出现两个转换器？**
见 §6 的升级表：把 `Packages/jp.lilxyzw.nontoon` 整个删掉重装一次。

---

## 8. 已知限制

- **只在 BiRP（VRChat）上保证可用**；URP 分支在 Unity 2022.3 上是死代码
- **VRChat 内做不到实时自阴影**：avatar 不能带相机/渲染纹理，所以自阴影只能是"烘焙那一刻的姿态"
- 转换是近似的，以下**未移植**：贴花（Decal）、溶解（Dissolve）、闪烁、荧光、宝石（Gem）、
  折射、AudioLink、UV 动画、视差深度、`_Emission2nd*`、渐变发光 —— 转换器会自动警告并跳过
- 两个 SubShader 都没声明 `Queue` 标签（见 §5）
- 官方 issue #7（Fur 在 Radeon / 老 N 卡上异常膨胀）**未处理**；上游作者说只能缓解、不能根治
- 面板里仍有约 48 条英文（ShaderCore 的底层属性，见 §5）
- **实时光源与 VRChat 性能等级冲突**：PC 上最高只能到 Poor（这是 VRChat 的规则，不是本包的缺陷）

---

## 依赖与许可

- 需要 **ShaderCore `jp.lilxyzw.shadercore` ≥ 0.1.9**（VPM 会自动带上）
- 修改部分基于 [lilxyzw/NonToon](https://github.com/lilxyzw/NonToon)，遵守其原始 LICENSE
- 材质转换器源自 **LilToonToNonToonConverter**（MIT，保留署名）

---

*本手册里的数字（属性条数、GUID、采样预算、中文覆盖率）都来自 2026-09 的实测，
不是估算；如果你发现与你的实际观察不符，欢迎反馈。*
